services:
  discord:
    build: ./discord
    environment:
      - POLARIS_TOKEN=${DISCORD_TOKEN}
      - POLARIS_NATION=${USER_AGENT}
      - POLARIS_REGION=${MAIN_REGION}
      - REDIS_OM_URL=redis://redis:6379
  redis:
    image: "redis:alpine"
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - ./redis:/data